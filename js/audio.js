// js/audio.js — browser-only. See PLAN.md Phase 10.
// Web Audio API synthesized SFX + looping music, no external files. Gated
// by Settings toggles (js/ui.js / localStorage). Must unlock AudioContext
// on a user gesture, not on page load.
